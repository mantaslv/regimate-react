import { Box, Typography } from "@mui/material";

const WorkoutDesignPage = () => {

    return (
        <Box sx={{ mt: 10 }}>
            <Typography variant="h5" color="primary" sx={{ textAlign: 'center', mb: 1 }}>Workout Design</Typography>
        </Box>
    );
};

export default WorkoutDesignPage;